apiVersion: v1
kind: Service
metadata:
  name: "{{ .Service.Name.DNSCase }}-read"
  namespace: "{{ .Namespace }}"
spec:
  selector:
    app: "{{ .Service.Unique.DNSCase}}-{{.ReadSelector}}"
  ports:
    - protocol: TCP
      name: tcp-port
      port: 7070
      targetPort: 6379
