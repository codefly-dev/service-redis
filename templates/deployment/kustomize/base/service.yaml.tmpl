apiVersion: v1
kind: Service
metadata:
  name: {{ .Service.Name.DNSCase }}
  namespace: "{{ .Namespace }}"
spec:
  selector:
    app: {{ .Service.Unique.DNSCase}}
  ports:
    - protocol: TCP
      name: tcp-port
      port: 7070
      targetPort: 6379
